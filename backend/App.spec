block_cipher = None

a = Analysis(
    ["server/server/wsgi.py"],
    pathex=['.', './server', './server/server', './server/api'],
    binaries=[],
    datas=[
        ('server/templates/', 'templates'),
        ('server/static/', 'static'),
        ('server/staticfiles/', 'staticfiles'),
    ],
    hiddenimports=[],
    hookspath=[],
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name="DjangoLauncher",
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=True,
    disable_windowed_traceback=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
